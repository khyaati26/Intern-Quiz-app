<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.softwareengmodel">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SoftwareEngModel">
        <activity android:name=".Expert_main"></activity>
        <activity android:name=".Expert_intro" />
        <activity android:name=".Intermediate_main" />
        <activity android:name=".Newbie_main" />
        <activity android:name=".Result" />
        <activity android:name=".Intermediate_intro" />
        <activity android:name=".StartPage" />
        <activity android:name=".Newbie_intro" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>